[build-system]
requires = [
  "scikit-build-core>=0.5",
  "meson-python>=0.14"
]
build-backend = "mesonpy"

[project]
name = "pyopenpivcore"
version = "0.1.0"

[tool.meson-python.args]
setup = ['-Dbuild_pybind=true']